services:
  anet-keygen:
    build:
      context: ./anet-keygen/
      dockerfile: Dockerfile
      args:
        APP_VERSION: master
    image: anet-keygen:latest
    container_name: anet-keygen

  anet-genconf:
    build:
      context: ./anet-genconf/
      dockerfile: Dockerfile
      args:
        APP_VERSION: latest
    image: anet-genconf:latest
    container_name: anet-genconf
    env_file:
      - .env
    depends_on:
      - anet-keygen #just for build image
    volumes:
      - ./generated-keys:/app/keys
      - ./config:/app/config
 
